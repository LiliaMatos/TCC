@model IEnumerable<PortariaInteligente.Models.Convite>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Visitantes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reunioes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Visitantes.VisitanteNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reunioes.ReuniaoNome)
            </td>
            <td>
                @* @Html.ActionLink("Edit", "Edit", new { id= item.VisitanteID+"_"+item.ReuniaoID})*@
                @Html.ActionLink("Details", "Details", new { id = item.VisitanteID + "_" + item.ReuniaoID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.VisitanteID + "_" + item.ReuniaoID })
            </td>
        </tr>
}
    </tbody>
</table>
